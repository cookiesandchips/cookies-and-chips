'use client';
import {useState} from 'react';
export default function Newsletter(){const [message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function act(action:string){setBusy(true);try{const token=new URLSearchParams(location.search).get('token');const r=await fetch('/api/newsletter',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,action})});const data=await r.json();setMessage(data.message||data.error);}catch{setMessage('Please try again later.');}finally{setBusy(false);}}return <section className="card"><h1>The Cookie Club</h1><p>Confirm your signup to receive news, offers, and a little sweetness from Cookies & Chips. You can unsubscribe at any time.</p><button disabled={busy} onClick={()=>act('confirm')}>Confirm my signup</button> <button disabled={busy} className="secondary" onClick={()=>act('unsubscribe')}>Unsubscribe</button><p role="status">{message}</p></section>}
