"use client";
import {useEffect,useRef} from 'react';
export function AdminDrawer({title,onClose,children,message,wide=false}:{wide?:boolean;message?:string;title:string;onClose:()=>void;children:React.ReactNode}){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const prior=document.activeElement as HTMLElement|null;ref.current?.showModal();return()=>{prior?.focus();};},[]);return <dialog ref={ref} className={"admin-drawer"+(wide?" admin-drawer-wide":"")} aria-labelledby="drawer-title" onCancel={e=>{e.preventDefault();onClose();}}><div className="drawer-heading"><div><p className="eyebrow">Details</p><h2 id="drawer-title">{title}</h2></div><button type="button" aria-label="Close details" onClick={onClose}>×</button></div><div className="drawer-body">{message&&<p className="message" role="status">{message}</p>}{children}</div></dialog>}
