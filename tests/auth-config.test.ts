import {test} from 'node:test';import assert from 'node:assert/strict';import {safeDestination,authConfig,SUPABASE_URL} from '../src/lib/auth-config';
test('callback destinations cannot leave the site or enter admin',()=>{for(const value of [null,'https://evil.test','//evil.test','/admin','/\\evil.test','%2f%2fevil.test'])assert.equal(safeDestination(value),'/account');assert.equal(safeDestination('/auth/update-password'),'/auth/update-password');});
test('authentication refuses another Supabase project and missing credentials',()=>{const oldUrl=process.env.NEXT_PUBLIC_SUPABASE_URL;const oldKey=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;try{process.env.NEXT_PUBLIC_SUPABASE_URL='https://other.supabase.co';process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY='test';assert.throws(authConfig);process.env.NEXT_PUBLIC_SUPABASE_URL=SUPABASE_URL;process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY='';assert.throws(authConfig);process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY='test';assert.equal(authConfig().url,SUPABASE_URL);}finally{if(oldUrl===undefined)delete process.env.NEXT_PUBLIC_SUPABASE_URL;else process.env.NEXT_PUBLIC_SUPABASE_URL=oldUrl;if(oldKey===undefined)delete process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;else process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=oldKey;}});
