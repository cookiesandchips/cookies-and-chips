import type {Order} from './server';
import {money} from './core';
// Keep support as a named monetary line. PayPal's DONATION category requires
// separate charity permissions; never classify the bakery as an approved charity.
export function paymentBody(o:Order,origin:string,sdk=false){return {intent:'CAPTURE',purchase_units:[{reference_id:o.id,custom_id:o.id,invoice_id:o.id,amount:{currency_code:'USD',value:money(o.total_cents),breakdown:{item_total:{currency_code:'USD',value:money(o.subtotal_cents+(o.donation_cents||0))},shipping:{currency_code:'USD',value:money(o.shipping_cents)},tax_total:{currency_code:'USD',value:money(o.tax_cents)}}},items:[...o.items.map(l=>({name:l.title+' ('+l.package_count+' per package)',quantity:String(l.quantity),unit_amount:{currency_code:'USD',value:money(l.unit_cents)}})),...(o.donation_cents?[{name:'CureSearch support',quantity:'1',unit_amount:{currency_code:'USD',value:money(o.donation_cents)}}]:[])],...(o.fulfillment.method!=='pickup'?{shipping:{name:{full_name:o.customer_name},address:{address_line_1:o.fulfillment.address.street1,address_line_2:o.fulfillment.address.street2||'',admin_area_2:o.fulfillment.address.city,admin_area_1:o.fulfillment.address.state,postal_code:o.fulfillment.address.zip,country_code:'US'}}}:{})}],...(sdk?{application_context:{brand_name:'Cookies & Chips',user_action:'PAY_NOW',shipping_preference:o.fulfillment.method==='pickup'?'NO_SHIPPING':'SET_PROVIDED_ADDRESS'}}:{payment_source:{paypal:{experience_context:{brand_name:'Cookies & Chips',user_action:'PAY_NOW',shipping_preference:o.fulfillment.method==='pickup'?'NO_SHIPPING':'SET_PROVIDED_ADDRESS',return_url:origin+'/checkout/return?order='+o.id,cancel_url:origin+'/checkout?cancelled=1'}}}})};}
