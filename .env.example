# Public identifiers; the URL is not a credential.
NEXT_PUBLIC_SUPABASE_URL=https://vqebxtybuiegvfxawksf.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
APP_ENV=development

# Server only. Provision only if privileged application operations require it.
SUPABASE_SECRET_KEY=

# Legacy one-time import only. Manage provider credentials and modes in bakery admin.
# Existing PayPal environment credentials are encrypted into bakery settings on first upgrade.
NEXT_PUBLIC_PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_WEBHOOK_ID=
PAYPAL_ENVIRONMENT=sandbox

# Resend selected; dedicated API key and verified sender still to be configured.
EMAIL_FROM=
EMAIL_PROVIDER_API_KEY=

# USDA stays in Vercel. TaxJar variables below are legacy one-time import only.
USDA_FDC_API_KEY=
TAXJAR_SANDBOX_API_TOKEN=
TAXJAR_LIVE_API_TOKEN=
TAXJAR_ENVIRONMENT=sandbox
TAXJAR_ENABLED=true

# Monitored customer support inbox
CONTACT_EMAIL=

# Legacy Shippo tokens: one-time import only; manage subsequent changes in bakery admin.
SHIPPO_TEST_API_TOKEN=
SHIPPO_LIVE_API_TOKEN=

# Infrastructure: server-only AES-256 key: 64 random hexadecimal characters. Keep stable after saving credentials.
INTEGRATION_ENCRYPTION_KEY=
