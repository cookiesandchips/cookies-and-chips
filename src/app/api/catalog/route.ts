import {db,failure} from '@/lib/checkout/server';
import {publicSite} from '@/lib/management/auth';
export const dynamic='force-dynamic';
export async function GET(){try{const [{data:products,error},site,{data:reviews}]=await Promise.all([db().from('commerce_products').select('id,title,price_cents,package_count,product_type,active,details'),publicSite(),db().from('commerce_reviews').select('id,display_name,rating,body').eq('status','approved').order('created_at',{ascending:false}).limit(20)]);if(error)throw error;const safeProducts=(products||[]).map(p=>(p.active?{...p,details:Object.fromEntries(['category','subcategory','collection','flavor','description','ingredients','allergens','defaultImage','image','featured'].map(k=>[k,p.details[k]]))}:{id:p.id,active:false,details:{}}));const safeSite=Object.fromEntries(['contactEmail','socials','monthlyProductId','storyIntro','storyBody','cancellationPolicy','allergenNotice'].map(k=>[k,site[k]]));return Response.json({products:safeProducts,site:safeSite,reviews:reviews||[]},{headers:{'Cache-Control':'no-store'}});}catch(e){return failure(e);}}
